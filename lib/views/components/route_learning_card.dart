import 'package:flutter/material.dart';

class RouteLearningCard extends StatelessWidget {
  const RouteLearningCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
